<?xml version="1.0"?>

<schema name="editor" version="1.5" xmlns:xi="http://www.w3.org/2001/XInclude">
    
  <!-- link to fieldType definitions for all cores-->
  <xi:include href="common/fieldtypes.xml" />
    <field name="id" type="int" indexed="true" stored="true" 
           required="true"/>
    <field name="editor" type="text_general" indexed="true" stored="true"
           required="true"/>
    <field name="bio" type="text_general" indexed="true" stored="true"/>

    <!-- Holds data for reponse writer -->
    <field name="_store" type="storefield" indexed="false" stored="true"/>
    <field name="_version_" type="long" indexed="true" stored="true"/>

  <!-- field to use to determine and enforce document uniqueness. -->
  <uniqueKey>id</uniqueKey>
  <!-- field for the QueryParser to use when an explicit fieldname is absent -->
  <defaultSearchField>editor</defaultSearchField>
</schema>

<!-- 
ID                ("_id",           MusicBrainzFieldTypes.TEXT_STORED_ANALYZED_NO_NORMS, new KeywordAnalyzer()),
EDITOR            ("editor",        MusicBrainzFieldTypes.TEXT_STORED_ANALYZED, new MusicbrainzAnalyzer()),
BIO               ("bio",           MusicBrainzFieldTypes.TEXT_STORED_ANALYZED, new MusicbrainzAnalyzer()),
EDITOR_STORE      ("editorstore",   MusicBrainzFieldTypes.TEXT_STORED_NOT_INDEXED),

├
-->