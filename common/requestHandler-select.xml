<requestHandler name="/select" class="solr.SearchHandler" default="true" >
  <lst name="defaults">
    <!-- changes made here may also need to be made in cores with custom requestHandlers -->
    <!-- these are: area, artist, label... -->
    <str name="echoParams">explicit</str>
    <str name="fl">score</str>
    <str name="df">catch</str>
  </lst>
</requestHandler>
