<?xml version="1.0"?>
<!-- This is a template for new cores. -->

<schema name="freedb" version="1.5" xmlns:xi="http://www.w3.org/2001/XInclude">

  <!--Searchable Fields
  artist, title, discid, cat, year, tracks
  -->

  <!-- link to fieldType definitions for all cores-->
  <xi:include href="common/fieldtypes.xml" />


  <field name="artist" type="text" indexed="true" stored="true"/>
  <field name="cat" type="lowercase" indexed="true" stored="true" omitNorms="true"/>
  <field name="discid" type="lowercase" indexed="true" stored="true" omitNorms="true"/>
  <field name="names" type="text_multi" indexed="true" stored="false" multiValued="true"/>
  <field name="title" type="title" indexed="true" stored="true"/>
  <field name="tracks" type="int" indexed="true" stored="true"/>
  <field name="year" type="int" indexed="true" stored="true"/>

  <!-- Holds data for reponse writer -->
  <field name="_store" type="storefield" indexed="false" stored="true"
         multiValued="false" required="false"/>

  <!-- If you remove this field, you must _also_ disable the update log in solrconfig.xml
     or Solr won't start. _version_ and update log are required for SolrCloud. -->
  <field name="_version_" type="long" indexed="true" stored="true"/>

  <copyField source="artist" dest="names" />
  <copyField source="title" dest="names" />


  <!-- field to use to determine and enforce document uniqueness. -->
  <uniqueKey>discid</uniqueKey>

  <!-- field for the QueryParser to use when an explicit fieldname is absent -->
  <defaultSearchField>names</defaultSearchField>

</schema>
